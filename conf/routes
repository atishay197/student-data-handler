# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

# Map static resources from the /public folder to the /assets URL path
#GET     /assets/*file               controllers.Assets.at(path="/public", file)
GET     /hello.html                 controllers.Assets.at(path="/public/Html",file="MainView.html")
POST    /storeData                  controllers.Application.storeData
GET     /getData                    controllers.Application.getData
GET     /DisplayRoll/*roll          controllers.Application.show(roll)